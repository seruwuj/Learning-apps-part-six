import javax.swing.JFrame;

public class JList {
    public static void main(String[] args) {
        TunaColorSelection go = new TunaColorSelection();
        go.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        go.setSize(300,200);
        go.setVisible(true);
    }
}
